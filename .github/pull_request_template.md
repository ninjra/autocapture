## Summary
- 

## Runtime Gates Checklist
- [ ] `make check` (CPU-only) is green
- [ ] `make check-gpu` run (or noted CUDA unavailable with probe output)
- [ ] `make bench-cpu` output attached
- [ ] `make bench-gpu` output attached (or status=skipped with reason)

## Notes
- Windows watchdog + runtime dir mapping validated
- Known limitations:
