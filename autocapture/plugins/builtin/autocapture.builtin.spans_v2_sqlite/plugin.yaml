schema_version: 1
plugin_id: autocapture.builtin.spans_v2_sqlite
name: Autocapture Builtin SQLite Spans V2 Backend
version: 1.0.0
description: Built-in SQLite spans_v2 backend.
enabled_by_default: true
extensions:
  - kind: spans_v2.backend
    id: sqlite
    name: SQLite
    aliases: [local]
    pillars:
      data_handling:
        cloud: none
        cloud_images: none
        supports_local: true
    factory:
      type: python
      entrypoint: autocapture.plugins.builtin.factories:create_spans_v2_backend_sqlite
